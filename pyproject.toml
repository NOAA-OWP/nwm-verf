[build-system]
build-backend = "setuptools.build_meta"
requires = [
  "setuptools>=42",
  "wheel",
]

[tool.ruff]
line-length = 88  

[tool.ruff.format]
quote-style = "double"   # optional: "single" or "double"
indent-style = "space"   # or "tab"
line-ending = "auto"     # or "lf"/"crlf"